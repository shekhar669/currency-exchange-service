Subject: Build Report for ${JOB_NAME} - Build #${BUILD_NUMBER}

Hello,

This is an automated message to inform you about the status of the build.
${myVariable2}
${myVariable1}
${FILE_PATH}
Job Name: ${JOB_NAME}
Build Number: ${BUILD_NUMBER}
Status: ${BUILD_STATUS}
Commit Message: $DEFAULT_CHANGELOG
Changes:
${CHANGES, showPaths=true, format="%a: %r %p"}

Thank you,
Jenkins

